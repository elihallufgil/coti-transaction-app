export enum TableNames {
  ACCOUNTS = 'accounts',
  ACTIONS = 'actions',
  ACTIVITIES = 'activities',
  NETWORKS = 'networks',
  TOKENS = 'tokens',
  APP_STATES = 'app_states',
  TRANSACTIONS = 'transactions',
}
